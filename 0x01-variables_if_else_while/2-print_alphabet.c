#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{
	int ch;

	char ch = 'a';

	for
		(ch <= 'z');
	putchar(ch, ch++);
	putchar('\n');
	getchar();
	return (0);
}
